import time

start=time.time()

def asal_mi(sayı):
    for i in range(2,sayı):
        if sayı%i==0:
            return False
            break
        else:
            return True            
        
    
def reverse(num):
    a=str(num)
    return int(a[::-1])
  
b=[i for i in range(3,100000,2) if not asal_mi(i) and not asal_mi(reverse(i))]
b.append(2)
print(b)
print(len(b))

print("it tooks ",(time.time()-start))
